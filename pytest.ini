[pytest]
testpaths =
    tests/unit
    tests/temp
markers =
    unit: testes unitários estáveis
    temp: testes temporários e exploratórios
addopts = 
    --cov=core 
    --cov=server 
    --cov-report=term-missing 
    --cov-report=html
